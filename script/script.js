window.onload= function() {
  Particles.init({
    selector: '.background',
    // Colors: #000000,
    maxParticles: 30,
    connectParticles: true,
    sizeVariations: 0,
    
  });
};

$(function(){
    $('.circlestat').circliful();
 });